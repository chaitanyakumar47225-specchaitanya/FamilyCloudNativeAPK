package com.familycloud.app;
public class AdminActivity extends BaseSimpleActivity {
    @Override protected void onCreate(android.os.Bundle b) {
        super.onCreate(b);
        showPage("Admin", "Admin login, drives, temperature, users, and disk movement will be added next.");
    }
}
