package com.familycloud.app;
public class BackupActivity extends BaseSimpleActivity {
    @Override protected void onCreate(android.os.Bundle b) {
        super.onCreate(b);
        showPage("Background Backup", "Sync Now, Enable Background Backup, and Free Up Space will be added next.");
    }
}
